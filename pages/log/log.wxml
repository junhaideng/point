<!--pages/log.wxml-->
<view wx:if="{{steps.length==0}}">
  <van-empty description="暂无日志" />
</view>
<view wx:else>
  <van-steps desc-class="text" steps="{{ steps }}" direction="vertical" active="{{-1}}" />
</view>